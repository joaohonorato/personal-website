# Quando as migrations Flyway estiverem criadas, trocar para:
#   spring.jpa.hibernate.ddl-auto: validate
#   spring.flyway.enabled: true
#   spring.flyway.locations: classpath:db/migration
spring:
  jpa:
    show-sql: false